import fs from 'node:fs';import zlib from 'node:zlib';import assert from 'node:assert/strict';globalThis.window=globalThis;
for(const f of ['mesh-core','template-core','template-sharp','mesh-repair','project-format'])await import('./dist/'+f+'.js');
const raw=zlib.gunzipSync(fs.readFileSync('dist/templates/ETSYFOLGER-print.mesh'));const p=SleeveTemplate.decode(raw.buffer.slice(raw.byteOffset,raw.byteOffset+raw.byteLength));const buf=zlib.gunzipSync(fs.readFileSync('test-fixtures/bottom-logo-mask.f32.gz'));const hm=new Float32Array(buf.buffer.slice(buf.byteOffset,buf.byteOffset+buf.byteLength));const design={heightmap:hm,cols:1024,rows:hm.length/1024,designWidth:22,designHeight:16.45,centerX:13,centerZ:0,designY:0};
for(const surface of ['underside','inside'])for(const negative of [false,true]){console.time(surface+negative);const r=SharpSleeve.buildAdaptive(p,{designs:[{...design,surface,...(surface==='inside'?{designWidth:13,designHeight:9.725,centerX:15.5,centerZ:7}:{designWidth:21,designHeight:15.71,centerX:14,centerZ:1})},{...design,surface:undefined,designWidth:20,designHeight:25,designY:45,designAngle:0},{...design,surface:undefined,designWidth:20,designHeight:25,designY:45,designAngle:180}],maxHeight:.6,negative,sharpSpacing:.14});const v=MeshCore.validateMesh(r.positions,r.indices);assert(r.info.affected>0);assert.equal(v.boundary,0);assert.equal(v.nonManifold,0);assert.equal(v.zeroArea,0);console.log(surface,negative,r.info.affected,v);console.timeEnd(surface+negative);}
